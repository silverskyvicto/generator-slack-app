{
  "name": "<%= slackAppName %>",
  "description": "<%= slackAppDescription %>",
  "version": "<%= slackAppVersion %>",
  "main": "<%= slackAppEntryPoint %>",
  "scripts": {
    "start": "node <%= slackAppEntryPoint %>",
    "test": "echo no test"
  },
  "dependencies": {
    "@slack/bolt": "^2.0.1",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
